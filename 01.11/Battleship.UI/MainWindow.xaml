<Window x:Class="Battleship.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Battleship.UI.ViewModels"
        xmlns:views="clr-namespace:Battleship.UI.Views"
        mc:Ignorable="d"
        Title="Морской бой (сеть)" Height="700" Width="1100" Background="#0b1021"
        WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:StartViewModel}">
            <views:StartView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:PlacementViewModel}">
            <views:PlacementView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:BattleViewModel}">
            <views:BattleView/>
        </DataTemplate>
    </Window.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#111827" Padding="12" BorderBrush="#1f2937" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Морской бой — локальная сеть" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,0,12,0"/>
                <TextBlock Text="{Binding GlobalStatus}" Foreground="#cbd5e1" VerticalAlignment="Center" TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <ContentControl Content="{Binding Current}" Margin="0"/>
    </DockPanel>
</Window>
