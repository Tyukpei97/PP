<UserControl x:Class="Battleship.UI.Views.BattleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Battleship.UI.ViewModels"
             xmlns:core="clr-namespace:Battleship.Core.Models;assembly=Battleship.Core"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <UserControl.Resources>
        <Style x:Key="BattleCellStyle" TargetType="Button">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Margin" Value="1.5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#1f2937"/>
            <Setter Property="Background" Value="#e5e7eb"/>
            <Setter Property="ToolTip" Value="Выстрел"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding State}" Value="{x:Static core:CellState.Ship}">
                    <Setter Property="Background" Value="#93c5fd"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="{x:Static core:CellState.Hit}">
                    <Setter Property="Background" Value="#f97316"/>
                    <Setter Property="Foreground" Value="White"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="{x:Static core:CellState.Sunk}">
                    <Setter Property="Background" Value="#b91c1c"/>
                    <Setter Property="Foreground" Value="White"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="{x:Static core:CellState.Miss}">
                    <Setter Property="Background" Value="#cbd5e1"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="#0f172a" Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <StackPanel Margin="0,0,20,0">
                <TextBlock Text="Сессия" Foreground="#cbd5e1"/>
                <TextBlock Text="{Binding SessionId}" Foreground="White" FontWeight="SemiBold"/>
            </StackPanel>
            <StackPanel Margin="0,0,20,0">
                <TextBlock Text="Статус" Foreground="#cbd5e1"/>
                <TextBlock Text="{Binding Status}" Foreground="White" FontWeight="SemiBold"/>
            </StackPanel>
            <StackPanel Margin="0,0,20,0">
                <TextBlock Text="Ход" Foreground="#cbd5e1"/>
                <TextBlock Text="{Binding TurnText}" Foreground="White" FontWeight="SemiBold"/>
            </StackPanel>
            <StackPanel>
                <TextBlock Text="Таймер" Foreground="#cbd5e1"/>
                <TextBlock Text="{Binding TimerText}" Foreground="White" FontWeight="SemiBold"/>
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="240"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#111827" Padding="10" CornerRadius="8" BorderBrush="#1f2937" BorderThickness="1" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="Ваше поле" Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,6"/>
                    <ItemsControl ItemsSource="{Binding OwnBoard.Cells}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Rows="10" Columns="10"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type vm:BoardCellViewModel}">
                                <Button Style="{StaticResource BattleCellStyle}"
                                        Command="{Binding ClickCommand}"
                                        CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="#0b1324" Padding="10" CornerRadius="8" BorderBrush="#1f2937" BorderThickness="1" Margin="8,0,8,0">
                <StackPanel>
                    <TextBlock Text="Поле соперника" Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,6"/>
                    <ItemsControl ItemsSource="{Binding OpponentBoard.Cells}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Rows="10" Columns="10"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type vm:BoardCellViewModel}">
                                <Button Style="{StaticResource BattleCellStyle}"
                                        Command="{Binding ClickCommand}"
                                        CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Text="Кликните по клетке для выстрела" Foreground="#cbd5e1" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Margin="8,0,0,0" Background="#111827" Padding="10" CornerRadius="8" BorderBrush="#1f2937" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Статистика" Foreground="White" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding StatsText}" Foreground="#cbd5e1" TextWrapping="Wrap" Margin="0,4,0,8"/>

                    <Separator/>
                    <TextBlock Text="Сохранения (только сервер)" Foreground="#cbd5e1" Margin="0,4,0,4"/>
                    <ComboBox ItemsSource="{Binding SaveFiles}" SelectedItem="{Binding SelectedSave}" IsEnabled="{Binding ShowSaves}" Margin="0,0,0,4"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <Button Content="Сохранить" Command="{Binding SaveCommand}" Padding="8,6" Background="#22c55e" Foreground="White" Margin="0,0,6,0"/>
                        <Button Content="Загрузить" Command="{Binding LoadCommand}" Padding="8,6" Background="#38bdf8" Foreground="White"/>
                    </StackPanel>

                    <Separator/>
                    <TextBlock Text="Потеряли связь?" Foreground="#cbd5e1" Margin="0,4,0,4"/>
                    <Button Content="Переподключиться" Command="{Binding ReconnectCommand}" Padding="8,6" Background="#f59e0b" Foreground="White"/>
                    <TextBlock Text="{Binding ReconnectStatus}" Foreground="#e0f2fe" TextWrapping="Wrap" Margin="0,4,0,4"/>

                    <Separator/>
                    <Button Content="Выйти в начало" Command="{Binding BackCommand}" Padding="8,6" Background="#ef4444" Foreground="White" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <TextBlock Grid.Row="2" Text="{Binding Status}" Foreground="#cbd5e1" Margin="4"/>
    </Grid>
</UserControl>
