<UserControl x:Class="InteractiveBuildingCrowdSimulator.App.Views.EditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:InteractiveBuildingCrowdSimulator.App.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="1040">
    <UserControl.Resources>
        <vm:EnumEqualsConverter x:Key="EnumEqualsConverter" />
        <vm:DoorCoordinateConverter x:Key="DoorCoordinateConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Background="#111827" Padding="8" BorderBrush="#1F2937" BorderThickness="0 0 0 1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Инструменты:" VerticalAlignment="Center" Margin="0 0 8 0" />
                <RadioButton Content="Выделение" Margin="4 0" IsChecked="{Binding SelectedTool, Converter={StaticResource EnumEqualsConverter}, ConverterParameter={x:Static vm:EditorTool.Select}}" />
                <RadioButton Content="Комната" Margin="4 0" IsChecked="{Binding SelectedTool, Converter={StaticResource EnumEqualsConverter}, ConverterParameter={x:Static vm:EditorTool.AddRoom}}" />
                <RadioButton Content="Коридор" Margin="4 0" IsChecked="{Binding SelectedTool, Converter={StaticResource EnumEqualsConverter}, ConverterParameter={x:Static vm:EditorTool.AddCorridor}}" />
                <RadioButton Content="Лестница" Margin="4 0" IsChecked="{Binding SelectedTool, Converter={StaticResource EnumEqualsConverter}, ConverterParameter={x:Static vm:EditorTool.AddStair}}" />
                <RadioButton Content="Дверь" Margin="4 0" IsChecked="{Binding SelectedTool, Converter={StaticResource EnumEqualsConverter}, ConverterParameter={x:Static vm:EditorTool.AddDoor}}" />
                <RadioButton Content="Препятствие" Margin="4 0" IsChecked="{Binding SelectedTool, Converter={StaticResource EnumEqualsConverter}, ConverterParameter={x:Static vm:EditorTool.AddObstacle}}" />
                <Separator Width="20" />
                <ToggleButton Content="Привязка к сетке" Margin="4 0" IsChecked="{Binding SnapToGrid}" />
                <TextBlock Text="Шаг:" VerticalAlignment="Center" Margin="8 0 4 0" />
                <TextBox Width="50" Text="{Binding GridSize}" Margin="0 0 4 0" />
                <Button Content="Проверить связность" Command="{Binding ValidateCommand}" />
                <Button Content="Удалить выбранное" Command="{Binding RemoveCommand}" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid Background="#0B1222" Margin="0 0 8 0">
                <Grid.Resources>
                    <Style TargetType="ItemsControl">
                        <Setter Property="ItemsPanel">
                            <Setter.Value>
                                <ItemsPanelTemplate>
                                    <Canvas x:Name="ItemsCanvas" />
                                </ItemsPanelTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ItemsControl">
                                    <ItemsPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Grid.Resources>

                <Canvas x:Name="MapCanvas"
                        Background="#0B1222"
                        ClipToBounds="True"
                        MouseLeftButtonDown="MapCanvas_OnMouseLeftButtonDown"
                        MouseMove="MapCanvas_OnMouseMove"
                        MouseLeftButtonUp="MapCanvas_OnMouseLeftButtonUp">
                    <ItemsControl ItemsSource="{Binding Map.Corridors}">
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Canvas.Left" Value="{Binding Bounds.X}" />
                                <Setter Property="Canvas.Top" Value="{Binding Bounds.Y}" />
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Rectangle Width="{Binding Bounds.Width}"
                                           Height="{Binding Bounds.Height}"
                                           Fill="#1E3A8A"
                                           Stroke="#60A5FA"
                                           StrokeThickness="1.5"
                                           RadiusX="2" RadiusY="2"
                                           Opacity="0.75" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <ItemsControl ItemsSource="{Binding Map.Rooms}">
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Canvas.Left" Value="{Binding Bounds.X}" />
                                <Setter Property="Canvas.Top" Value="{Binding Bounds.Y}" />
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="{Binding Bounds.Width}"
                                        Height="{Binding Bounds.Height}"
                                        Background="#22C55E33"
                                        BorderBrush="#22C55E"
                                        BorderThickness="1.5"
                                        CornerRadius="2">
                                    <TextBlock Text="{Binding Name}" Foreground="White" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Center" />
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <ItemsControl ItemsSource="{Binding Map.Stairs}">
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Canvas.Left" Value="{Binding Bounds.X}" />
                                <Setter Property="Canvas.Top" Value="{Binding Bounds.Y}" />
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="{Binding Bounds.Width}"
                                        Height="{Binding Bounds.Height}"
                                        Background="#F59E0B33"
                                        BorderBrush="#FBBF24"
                                        BorderThickness="1.5"
                                        CornerRadius="2">
                                    <TextBlock Text="{Binding Name}" Foreground="White" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Center" />
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <ItemsControl ItemsSource="{Binding Map.Obstacles}">
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Canvas.Left" Value="{Binding Bounds.X}" />
                                <Setter Property="Canvas.Top" Value="{Binding Bounds.Y}" />
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Rectangle Width="{Binding Bounds.Width}"
                                           Height="{Binding Bounds.Height}"
                                           Fill="#991B1B"
                                           Stroke="#F87171"
                                           StrokeThickness="1" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <ItemsControl ItemsSource="{Binding Map.Doors}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Line Stroke="#FACC15" StrokeThickness="2" StrokeDashArray="4 2">
                                    <Line.X1>
                                        <MultiBinding Converter="{StaticResource DoorCoordinateConverter}" ConverterParameter="FromX">
                                            <Binding />
                                            <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="DataContext" />
                                        </MultiBinding>
                                    </Line.X1>
                                    <Line.Y1>
                                        <MultiBinding Converter="{StaticResource DoorCoordinateConverter}" ConverterParameter="FromY">
                                            <Binding />
                                            <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="DataContext" />
                                        </MultiBinding>
                                    </Line.Y1>
                                    <Line.X2>
                                        <MultiBinding Converter="{StaticResource DoorCoordinateConverter}" ConverterParameter="ToX">
                                            <Binding />
                                            <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="DataContext" />
                                        </MultiBinding>
                                    </Line.X2>
                                    <Line.Y2>
                                        <MultiBinding Converter="{StaticResource DoorCoordinateConverter}" ConverterParameter="ToY">
                                            <Binding />
                                            <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="DataContext" />
                                        </MultiBinding>
                                    </Line.Y2>
                                </Line>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Canvas>
            </Grid>

            <Border Grid.Column="1" Background="#111827" Padding="12">
                <StackPanel>
                    <TextBlock Text="Свойства" FontSize="16" FontWeight="Bold" />
                    <TextBlock Text="Выбранный объект:" Margin="0 6 0 0" />
                    <TextBlock Text="{Binding SelectedArea.Name}" FontWeight="SemiBold" />
                    <TextBlock Text="Положение (X,Y):" Margin="0 6 0 0" />
                    <TextBlock Text="{Binding SelectedArea.Bounds.X, StringFormat=0.##}" />
                    <TextBlock Text="{Binding SelectedArea.Bounds.Y, StringFormat=0.##}" />
                    <Separator Margin="0 8 0 8" />
                    <TextBlock Text="Сообщение:" />
                    <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
