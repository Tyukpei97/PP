<Window x:Class="InteractiveBuildingCrowdSimulator.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:InteractiveBuildingCrowdSimulator.App.ViewModels"
        xmlns:views="clr-namespace:InteractiveBuildingCrowdSimulator.App.Views"
        mc:Ignorable="d"
        Title="Симулятор движения толпы в здании"
        Width="1280" Height="860"
        WindowStartupLocation="CenterScreen"
        Background="#0F172A"
        Foreground="White">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Window.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#38BDF8" />
        <SolidColorBrush x:Key="PanelBrush" Color="#111827" />
        <SolidColorBrush x:Key="CardBrush" Color="#1F2937" />
        <Style TargetType="Button">
            <Setter Property="Margin" Value="4" />
            <Setter Property="Padding" Value="10 8" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="#1F2937" />
            <Setter Property="BorderBrush" Value="#334155" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <DataTemplate DataType="{x:Type vm:EditorViewModel}">
            <views:EditorView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SimulationViewModel}">
            <views:SimulationView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:AnalyticsViewModel}">
            <views:AnalyticsView />
        </DataTemplate>
    </Window.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Top" Background="{StaticResource PanelBrush}" Padding="12" BorderBrush="#1F2937" BorderThickness="0 0 0 1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Интерактивный симулятор толпы" FontSize="20" FontWeight="Bold" Margin="0 0 8 0" />
                    <Button Content="Импорт JSON" Command="{Binding ImportCommand}" />
                    <Button Content="Экспорт JSON" Command="{Binding ExportCommand}" />
                    <Button Content="Загрузить пример" Command="{Binding LoadSampleCommand}" />
                </StackPanel>
                <StackPanel Grid.Column="1" HorizontalAlignment="Right" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Статус:" Opacity="0.7" Margin="0 0 6 0" />
                    <TextBlock Text="{Binding Status}" FontWeight="SemiBold" />
                </StackPanel>
            </Grid>
        </Border>

        <Border DockPanel.Dock="Left" Background="{StaticResource PanelBrush}" Padding="10" Width="230" BorderBrush="#1F2937" BorderThickness="0 0 1 0">
            <StackPanel>
                <TextBlock Text="Навигация" FontSize="16" FontWeight="Bold" Margin="0 0 0 8" />
                <Button Content="Редактор карты" Command="{Binding ShowEditorCommand}" />
                <Button Content="Симуляция" Command="{Binding ShowSimulationCommand}" />
                <Button Content="Аналитика" Command="{Binding ShowAnalyticsCommand}" />
                <Separator Margin="0 12 0 12" />
                <TextBlock Text="Подсказки" FontWeight="Bold" />
                <TextBlock Text="• Все подписи только на русском." TextWrapping="Wrap" Opacity="0.7" />
                <TextBlock Text="• Симуляция выполняется в фоне, UI не блокируется." TextWrapping="Wrap" Opacity="0.7" />
                <TextBlock Text="• Импорт/экспорт только в JSON." TextWrapping="Wrap" Opacity="0.7" />
            </StackPanel>
        </Border>

        <Border Background="{StaticResource CardBrush}" Padding="12">
            <ContentControl Content="{Binding CurrentViewModel}" />
        </Border>
    </DockPanel>
</Window>
