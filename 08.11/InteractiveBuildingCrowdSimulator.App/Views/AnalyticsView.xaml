<UserControl x:Class="InteractiveBuildingCrowdSimulator.App.Views.AnalyticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:InteractiveBuildingCrowdSimulator.App.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="1040">
    <UserControl.Resources>
        <vm:ChartSeriesConverter x:Key="ChartSeriesConverter" />
    </UserControl.Resources>

    <Grid Background="#0B1222" Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Аналитика и графики" FontSize="20" FontWeight="Bold" Margin="0 0 0 8" />

        <UniformGrid Grid.Row="1" Rows="2" Columns="2" Margin="0 8 0 0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Border Background="#111827" Padding="8" Margin="6" CornerRadius="4" BorderBrush="#1F2937" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Оставшиеся агенты" FontWeight="Bold" />
                    <Grid Height="200">
                        <Grid x:Name="AgentsChart">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Polyline Stroke="#38BDF8" StrokeThickness="2">
                                <Polyline.Points>
                                    <MultiBinding Converter="{StaticResource ChartSeriesConverter}">
                                        <Binding Path="AgentsRemaining" />
                                        <Binding Path="ActualWidth" RelativeSource="{RelativeSource AncestorType=Grid}" />
                                        <Binding Path="ActualHeight" RelativeSource="{RelativeSource AncestorType=Grid}" />
                                    </MultiBinding>
                                </Polyline.Points>
                            </Polyline>
                        </Grid>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Background="#111827" Padding="8" Margin="6" CornerRadius="4" BorderBrush="#1F2937" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Средняя скорость" FontWeight="Bold" />
                    <Grid Height="200">
                        <Polyline Stroke="#A78BFA" StrokeThickness="2">
                            <Polyline.Points>
                                <MultiBinding Converter="{StaticResource ChartSeriesConverter}">
                                    <Binding Path="AverageSpeed" />
                                    <Binding Path="ActualWidth" RelativeSource="{RelativeSource AncestorType=Grid}" />
                                    <Binding Path="ActualHeight" RelativeSource="{RelativeSource AncestorType=Grid}" />
                                </MultiBinding>
                            </Polyline.Points>
                        </Polyline>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Background="#111827" Padding="8" Margin="6" CornerRadius="4" BorderBrush="#1F2937" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Максимальная плотность" FontWeight="Bold" />
                    <Grid Height="200">
                        <Polyline Stroke="#F472B6" StrokeThickness="2">
                            <Polyline.Points>
                                <MultiBinding Converter="{StaticResource ChartSeriesConverter}">
                                    <Binding Path="MaxDensity" />
                                    <Binding Path="ActualWidth" RelativeSource="{RelativeSource AncestorType=Grid}" />
                                    <Binding Path="ActualHeight" RelativeSource="{RelativeSource AncestorType=Grid}" />
                                </MultiBinding>
                            </Polyline.Points>
                        </Polyline>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Background="#111827" Padding="8" Margin="6" CornerRadius="4" BorderBrush="#1F2937" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="Проблемные зоны" FontWeight="Bold" />
                    <TextBlock Text="{Binding ProblemAreas}" TextWrapping="Wrap" Margin="0 4 0 0" />
                    <TextBlock Text="Советы:" FontWeight="Bold" Margin="0 8 0 2" />
                    <TextBlock Text="• Добавьте выходы или расширьте двери." TextWrapping="Wrap" Opacity="0.8" Margin="0 2 0 0" />
                    <TextBlock Text="• Избегайте узких коридоров при высокой плотности." TextWrapping="Wrap" Opacity="0.8" Margin="0 2 0 0" />
                    <TextBlock Text="• Проверяйте связность перед запуском." TextWrapping="Wrap" Opacity="0.8" Margin="0 2 0 0" />
                </StackPanel>
            </Border>
        </UniformGrid>
    </Grid>
</UserControl>
