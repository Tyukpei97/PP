<UserControl x:Class="TmSimulator.UI.Views.MachineWorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:TmSimulator.UI.ViewModels"
             xmlns:render="clr-namespace:TmSimulator.UI.Rendering"
             xmlns:machine="clr-namespace:TmSimulator.Core.Machine"
             mc:Ignorable="d"
             d:DesignWidth="1200" d:DesignHeight="800">
    <UserControl.Resources>
        <render:EdgeGeometryConverter x:Key="EdgeGeometryConverter"/>
        <render:BooleanToBrushConverter x:Key="BooleanToBrushConverter"/>
        <render:BoolToStartConverter x:Key="BoolToStart"/>
        <render:DirectionToTextConverter x:Key="DirectionToTextConverter"/>
        <render:SimulationStatusConverter x:Key="SimulationStatusConverter"/>
        <Style TargetType="Border" x:Key="StateBorder">
            <Setter Property="Width" Value="70"/>
            <Setter Property="Height" Value="70"/>
            <Setter Property="CornerRadius" Value="35"/>
            <Setter Property="Background" Value="#2E86DE"/>
            <Setter Property="BorderBrush" Value="#1B4F72"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Opacity" Value="0.9"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.4*"/>
            <ColumnDefinition Width="1.6*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Margin="10" Padding="8" Background="#0F172A" CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Диаграмма состояний" Foreground="White" FontSize="16" FontWeight="Bold" Margin="4"/>
                <ScrollViewer Grid.Row="1">
                    <Grid>
                        <ItemsControl ItemsSource="{Binding Rules}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type vm:TransitionViewModel}">
                                    <Grid>
                                        <Path Stroke="#00B894" StrokeThickness="2" Opacity="0.7">
                                            <Path.Data>
                                                <MultiBinding Converter="{StaticResource EdgeGeometryConverter}">
                                                    <Binding Path="FromState.X"/>
                                                    <Binding Path="FromState.Y"/>
                                                    <Binding Path="ToState.X"/>
                                                    <Binding Path="ToState.Y"/>
                                                </MultiBinding>
                                            </Path.Data>
                                        </Path>
                                        <TextBlock Foreground="White" FontSize="12" FontWeight="SemiBold">
                                            <TextBlock.RenderTransform>
                                                <TranslateTransform X="{Binding FromState.X}" Y="{Binding FromState.Y}"/>
                                            </TextBlock.RenderTransform>
                                            <TextBlock.Text>
                                                <MultiBinding StringFormat="{}{0} → {1}">
                                                    <Binding Path="From"/>
                                                    <Binding Path="To"/>
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <ItemsControl ItemsSource="{Binding States}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type vm:StateViewModel}">
                                    <Grid>
                                        <Border Style="{StaticResource StateBorder}"
                                                Canvas.Left="{Binding X}"
                                                Canvas.Top="{Binding Y}"
                                                Background="{Binding IsHalting, Converter={StaticResource BooleanToBrushConverter}, ConverterParameter=Halting}">
                                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" Foreground="White" HorizontalAlignment="Center" FontWeight="Bold"/>
                                                <TextBlock Text="{Binding IsStart, Converter={StaticResource BoolToStart}}" Foreground="#FFD166" FontSize="12" HorizontalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </ScrollViewer>
            </Grid>
        </Border>

        <ScrollViewer Grid.Column="1" Margin="10" VerticalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical">
                <Border Background="#0B253D" Padding="8" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Статус" Foreground="White" FontWeight="Bold"/>
                        <TextBlock Text="{Binding StatusMessage}" Foreground="#BBE1FA" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <Border Background="#0B253D" Padding="8" CornerRadius="8">
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="Алфавит и пустой символ" Foreground="White" FontWeight="Bold"/>
                        <ItemsControl ItemsSource="{Binding AlphabetSymbols}" Margin="0 4 8 4">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#123456" Padding="4" CornerRadius="4" Margin="2">
                                        <TextBlock Text="{Binding}" Foreground="White"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <StackPanel Orientation="Horizontal" Margin="0 4 0 0">
                            <TextBlock Text="Новый символ:" Foreground="White" VerticalAlignment="Center"/>
                            <TextBox Width="60" x:Name="NewSymbolBox"/>
                            <Button Content="Добавить" Command="{Binding AddSymbolCommand}" CommandParameter="{Binding Text, ElementName=NewSymbolBox}" />
                            <Button Content="Удалить" Command="{Binding RemoveSymbolCommand}" CommandParameter="{Binding Text, ElementName=NewSymbolBox}" />
                        </StackPanel>
                        <CheckBox Content="Детерминированный режим" IsChecked="{Binding Deterministic}" Foreground="White"/>
                    </StackPanel>
                </Border>

                <Border Background="#0B253D" Padding="8" CornerRadius="8">
                    <StackPanel Margin="0 6 0 0">
                        <TextBlock Text="Состояния" Foreground="White" FontWeight="Bold"/>
                        <StackPanel Orientation="Horizontal" Margin="0 4 0 0">
                            <TextBox Width="100" x:Name="StateNameBox" ToolTip="Имя состояния"/>
                            <Button Content="Добавить состояние" Command="{Binding AddStateCommand}" CommandParameter="{Binding Text, ElementName=StateNameBox}"/>
                        </StackPanel>
                        <ItemsControl ItemsSource="{Binding States}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#11202F" Padding="6" CornerRadius="4" Margin="2">
                                        <StackPanel Orientation="Horizontal" Margin="0 0 0 0">
                                            <TextBlock Text="{Binding Name}" Foreground="White" Width="80"/>
                                            <Button Content="Старт" Command="{Binding DataContext.SetStartStateCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding Name}"/>
                                            <Button Content="Финиш" Command="{Binding DataContext.ToggleHaltingCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding Name}"/>
                                            <Button Content="Удалить" Command="{Binding DataContext.RemoveStateCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding Name}"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Background="#0B253D" Padding="8" CornerRadius="8">
                    <StackPanel Margin="0 6 0 0">
                        <TextBlock Text="Правила переходов" Foreground="White" FontWeight="Bold"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Vertical" Grid.Column="0">
                                <WrapPanel Margin="0 4 0 0">
                                    <TextBlock Text="Из" Foreground="White" VerticalAlignment="Center"/>
                                    <ComboBox Width="100" ItemsSource="{Binding States}" SelectedValuePath="Name" SelectedValue="{Binding NewRuleFrom, Mode=TwoWay}" DisplayMemberPath="Name"/>
                                    <TextBlock Text="в" Foreground="White" VerticalAlignment="Center"/>
                                    <ComboBox Width="100" ItemsSource="{Binding States}" SelectedValuePath="Name" SelectedValue="{Binding NewRuleTo, Mode=TwoWay}" DisplayMemberPath="Name"/>
                                </WrapPanel>
                                <WrapPanel Margin="0 4 0 0">
                                    <TextBlock Text="Читает" Foreground="White" VerticalAlignment="Center"/>
                                    <TextBox Width="50" Text="{Binding NewRuleRead, Mode=TwoWay}"/>
                                    <TextBlock Text="Пишет" Foreground="White" VerticalAlignment="Center"/>
                                    <TextBox Width="50" Text="{Binding NewRuleWrite, Mode=TwoWay}"/>
                                    <TextBlock Text="Движение" Foreground="White" VerticalAlignment="Center"/>
                                    <ComboBox Width="90" SelectedValuePath="Tag" SelectedValue="{Binding NewRuleMove, Mode=TwoWay}">
                                        <ComboBoxItem Content="Влево" Tag="{x:Static machine:Direction.Left}"/>
                                        <ComboBoxItem Content="Вправо" Tag="{x:Static machine:Direction.Right}"/>
                                        <ComboBoxItem Content="Стоять" Tag="{x:Static machine:Direction.Stay}"/>
                                    </ComboBox>
                                    <Button Content="Добавить правило" Command="{Binding ApplyNewRuleCommand}"/>
                                </WrapPanel>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Текстовый импорт (каждое правило на новой строке)" Foreground="White"/>
                                <TextBox Text="{Binding RulesText}" AcceptsReturn="True" Height="90" VerticalScrollBarVisibility="Auto"/>
                                <Button Content="Импортировать" Command="{Binding ImportRulesCommand}" Margin="0 4 0 0"/>
                            </StackPanel>
                        </Grid>
                        <DataGrid ItemsSource="{Binding Rules}" AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False" Height="150">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Из" Binding="{Binding From}"/>
                                <DataGridTextColumn Header="Читает" Binding="{Binding Read}"/>
                                <DataGridTextColumn Header="В" Binding="{Binding To}"/>
                                <DataGridTextColumn Header="Пишет" Binding="{Binding Write}"/>
                                <DataGridTextColumn Header="Шаг" Binding="{Binding Move, Converter={StaticResource DirectionToTextConverter}}"/>
                                <DataGridTextColumn Header="Метка" Binding="{Binding Label}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <Border Background="#0B253D" Padding="8" CornerRadius="8">
                    <StackPanel Margin="0 6 0 0">
                        <TextBlock Text="Лента и управление" Foreground="White" FontWeight="Bold"/>
                        <StackPanel Orientation="Horizontal" Margin="0 4 0 0">
                            <TextBlock Text="Ввод на ленту:" Foreground="White" VerticalAlignment="Center"/>
                            <TextBox Width="200" Text="{Binding TapeInput, Mode=TwoWay}"/>
                            <TextBlock Text="Лимит шагов" Foreground="White" VerticalAlignment="Center"/>
                            <TextBox Width="80" Text="{Binding StepLimit, Mode=TwoWay}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0 4 0 0" VerticalAlignment="Center">
                            <TextBlock Text="Задержка между шагами (мс)" Foreground="White" VerticalAlignment="Center"/>
                            <Slider Width="180" Minimum="0" Maximum="1000" Value="{Binding DelayMs, Mode=TwoWay}"/>
                            <TextBlock Text="{Binding DelayMs}" Foreground="White" Width="40"/>
                        </StackPanel>
                        <ItemsControl ItemsSource="{Binding TapeCells}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="17"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderThickness="1" BorderBrush="#1B4F72" Background="{Binding IsHead, Converter={StaticResource BooleanToBrushConverter}, ConverterParameter=Head}" Padding="4" Margin="2">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Position}" Foreground="#7FDBFF" FontSize="10" HorizontalAlignment="Center"/>
                                            <TextBlock Text="{Binding Symbol}" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <WrapPanel Margin="0 6 0 0">
                            <Button Content="Запустить" Command="{Binding StartCommand}"/>
                            <Button Content="Шаг" Command="{Binding StepCommand}"/>
                            <Button Content="Пауза" Command="{Binding PauseCommand}"/>
                            <Button Content="Сброс" Command="{Binding ResetCommand}"/>
                            <Button Content="Быстрый прогон" Command="{Binding FastForwardCommand}"/>
                        </WrapPanel>
                    </StackPanel>
                </Border>

                <Border Background="#0B253D" Padding="8" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Трассировка" Foreground="White" FontWeight="Bold"/>
                        <DataGrid ItemsSource="{Binding TraceEntries}" AutoGenerateColumns="False" CanUserAddRows="False" Height="160">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Шаг" Binding="{Binding Step}"/>
                                <DataGridTextColumn Header="Состояние" Binding="{Binding State}"/>
                                <DataGridTextColumn Header="Читает" Binding="{Binding Read}"/>
                                <DataGridTextColumn Header="Пишет" Binding="{Binding Write}"/>
                                <DataGridTextColumn Header="Движение" Binding="{Binding Move, Converter={StaticResource DirectionToTextConverter}}"/>
                                <DataGridTextColumn Header="Позиция" Binding="{Binding HeadPosition}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <Button Content="Экспорт в TXT" Command="{Binding ExportTraceCommand}" Margin="0 4 0 0"/>
                    </StackPanel>
                </Border>

                <Border Background="#0B253D" Padding="8" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Подсказки по завершению" Foreground="White" FontWeight="Bold"/>
                        <ItemsControl ItemsSource="{Binding HeuristicHints}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="•" Foreground="#FFD166" Margin="0 0 4 0"/>
                                        <TextBlock Text="{Binding}" Foreground="White" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Background="#0B253D" Padding="8" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Результаты тестов" Foreground="White" FontWeight="Bold"/>
                        <Button Content="Запустить тесты" Command="{Binding RunTestsCommand}" CommandParameter="{Binding DataContext.TestInputs, RelativeSource={RelativeSource AncestorType=Window}}" />
                        <DataGrid ItemsSource="{Binding TestResults}" AutoGenerateColumns="False" CanUserAddRows="False" Height="160" Margin="0 4 0 0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Ввод" Binding="{Binding Input}"/>
                                <DataGridTextColumn Header="Статус" Binding="{Binding Status, Converter={StaticResource SimulationStatusConverter}}"/>
                                <DataGridTextColumn Header="Шаги" Binding="{Binding Steps}"/>
                                <DataGridTextColumn Header="Выход" Binding="{Binding Output}"/>
                                <DataGridTextColumn Header="Сообщение" Binding="{Binding Message}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <Border Background="#0B253D" Padding="8" CornerRadius="8">
                    <StackPanel Orientation="Horizontal" Margin="0 6 0 0">
                        <Button Content="Сохранить проект" Command="{Binding SaveProjectCommand}" CommandParameter="{Binding DataContext.TestInputs, RelativeSource={RelativeSource AncestorType=Window}}"/>
                        <Button Content="Загрузить проект" Command="{Binding LoadProjectCommand}" CommandParameter="{Binding DataContext.TestInputs, RelativeSource={RelativeSource AncestorType=Window}}"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
